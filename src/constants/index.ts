import { COLORS, SIZES } from './theme';

const LANGUAGES: TLanguage[] = ['es', 'en'];
const THEME_MODES: TThemeMode[] = ['system', 'light', 'dark'];

export { COLORS, SIZES, LANGUAGES, THEME_MODES };
