import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    borderRadius: 16,
    height: 180,
    marginTop: 40,
    width: '100%',
  },
});

export default styles;
